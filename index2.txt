<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Laser Art on Wood | Catálogo</title>

  <style>
    /* --- Variables y Configuración General --- */
    :root {
      --bg: #f4f6f8;
      --card: #ffffff;
      --card-soft: #f9fafb;
      --text: #1f2937;
      --muted: #6b7280;
      --line: #e5e7eb;
      --accent: #2563eb;
      --accent2: #16a34a;
      --shadow: 0 10px 25px rgba(0, 0, 0, .08);
      --radius: 18px;
      --radius2: 14px;
      --max: 1100px;
      --font: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Noto Sans", sans-serif;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: var(--font);
      color: var(--text);
      background:
        radial-gradient(1200px 600px at 20% 0%, rgba(37, 99, 235, .08), transparent 55%),
        radial-gradient(900px 500px at 80% 10%, rgba(22, 163, 74, .08), transparent 50%),
        var(--bg);
    }

    a { text-decoration: none; color: inherit; }
    .wrap { max-width: var(--max); margin: auto; padding: 24px; }

    /* --- Header --- */
    header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; }
    .brand { display: flex; align-items: center; gap: 12px; }
    .logo { width: 42px; height: 42px; border-radius: 12px; background: linear-gradient(135deg, var(--accent), var(--accent2)); }
    
    nav a {
      margin-left: 14px; padding: 8px 14px; border-radius: 999px;
      border: 1px solid var(--line); background: var(--card); font-size: 14px;
      transition: all 0.2s ease;
    }
    nav a:hover { background: var(--accent); color: white; border-color: var(--accent); }

    /* --- Hero Section --- */
    .hero {
      background: var(--card); border-radius: var(--radius);
      box-shadow: var(--shadow); padding: 28px; margin-bottom: 32px;
    }
    .hero h1 { margin-top: 0; }
    .pill {
      display: inline-block; padding: 6px 12px; border-radius: 999px;
      background: var(--card-soft); border: 1px solid var(--line);
      font-size: 13px; margin-right: 8px; margin-top: 8px;
    }

    .section-title { margin: 40px 0 16px; }

    /* --- Grid (Rejilla de tarjetas) --- */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 18px;
    }

    /* --- Estilos Base Tarjeta --- */
    .card {
      background: var(--card); border: 1px solid var(--line);
      border-radius: var(--radius2); padding: 18px;
      box-shadow: var(--shadow); position: relative; overflow: hidden;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .card:hover { transform: translateY(-3px); box-shadow: 0 15px 30px rgba(0, 0, 0, .12); }
    .card h3 { margin-top: 0; margin-bottom: 10px; }
    .card p { margin-bottom: 0; color: var(--muted); font-size: 15px; line-height: 1.5; }

    /* =========================================
       FONDOS PERSONALIZADOS (IMÁGENES)
    ========================================= */

    /* 1. Decoración */
    .card-deco {
      background: linear-gradient(rgba(255, 255, 255, 0.85), rgba(255, 255, 255, 0.85)), url('fondo.png');
      background-size: cover; background-position: center; background-repeat: no-repeat;
      background-origin: border-box;
    }

    /* 2. Cajas */
    .card-cajas {
      background: linear-gradient(rgba(255, 255, 255, 0.85), rgba(255, 255, 255, 0.85)), url('fondo2.png');
      background-size: cover; background-position: center; background-repeat: no-repeat;
      background-origin: border-box;
    }

    /* 3. Figuras 3D */
    .card-3d {
      background: linear-gradient(rgba(255, 255, 255, 0.85), rgba(255, 255, 255, 0.85)), url('fondo3.png');
      background-size: cover; background-position: center; background-repeat: no-repeat;
      background-origin: border-box;
    }

    /* 4. Regalos Personalizados (NUEVA) */
    .card-regalos {
      background: linear-gradient(rgba(255, 255, 255, 0.85), rgba(255, 255, 255, 0.85)), url('fondo4.png');
      background-size: cover; background-position: center; background-repeat: no-repeat;
      background-origin: border-box;
    }
    /* ========================================= */

    footer {
      margin-top: 40px; padding: 24px; text-align: center;
      font-size: 14px; color: var(--muted); border-top: 1px solid var(--line);
    }
  </style>
</head>

<body>
  <div class="wrap">

    <header>
      <div class="brand">
        <div class="logo"></div>
        <div>
          <strong>Laser Art on Wood</strong><br>
          <small>Corte y grabado láser en madera</small>
        </div>
      </div>
      <nav>
        <a href="#categorias">Categorías</a>
        <a href="#catalogo">Catálogo</a>
        <a href="#contacto">Contacto</a>
      </nav>
    </header>

    <section class="hero">
      <h1>Catálogo de productos en madera</h1>
      <p>Diseños personalizados, multicapa y funcionales.</p>
      <span class="pill">Corte plano</span>
      <span class="pill">Multicapa / 3D</span>
      <span class="pill">Personalizado</span>
    </section>

    <section id="categorias">
      <div class="section-title">
        <h2>Categorías principales</h2>
        <p style="margin-top:8px; color:var(--muted)">Explora los diferentes tipos de trabajos.</p>
      </div>

      <div class="grid">
        <div class="card card-deco">
          <h3>Decoración en madera</h3>
          <p>Pesebres, adornos, centros de mesa y decoración por temporada.</p>
        </div>

        <div class="card card-cajas">
          <h3>Organizadores y cajas</h3>
          <p>Cajas, estuches, joyeros y organizadores funcionales.</p>
        </div>

        <div class="card card-3d">
          <h3>Figuras armables / 3D</h3>
          <p>Barcos, modelos armables, dinosaurios y proyectos complejos.</p>
        </div>

        <div class="card card-regalos">
          <h3>Regalos personalizados</h3>
          <p>Nombres, fechas, llaveros y diseños por encargo.</p>
        </div>

        <div class="card">
          <h3>Arte y cultura pop</h3>
          <p>Cuadros multicapa, anime y cultura geek.</p>
        </div>
        
        <div class="card">
          <h3>Iluminación decorativa</h3>
          <p>Diseños preparados para luz LED.</p>
        </div>

      </div>
    </section>

    <section id="catalogo" style="margin-top:60px">
      <div class="section-title">
        <h2>Catálogo (Ejemplos)</h2>
      </div>
      <div class="grid">
        <div class="card">
          <h3>Pesebre multicapa</h3>
          <p>Decoración navideña.</p>
          <strong>Desde $15.000</strong>
        </div>
        <div class="card">
          <h3>Caja organizadora</h3>
          <p>Caja armable para regalos.</p>
          <strong>Desde $8.500</strong>
        </div>
        <div class="card">
          <h3>Figura armable 3D</h3>
          <p>Modelo decorativo tipo barco.</p>
          <strong>A pedido</strong>
        </div>
      </div>
    </section>

    <section id="contacto" style="margin-top:60px">
      <div class="section-title"><h2>Contacto</h2></div>
      <p>Escríbeme para cotizaciones o pedidos personalizados.</p>
      <p>
        📱 WhatsApp: +56 9 XXXXXXXX<br>
        📸 Instagram: @tu_usuario
      </p>
    </section>

    <footer>Laser Art on Wood · Catálogo Web</footer>

  </div>
</body>
</html>